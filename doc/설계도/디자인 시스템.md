## 6. 디자인 시스템

> `design-system/tokens.ts` 에 정의. 컴포넌트는 직접 색상값 하드코딩 금지, 반드시 토큰 참조.
> 

### 6-1. 컬러 토큰

| 그룹 | 역할 | 특성 |
| --- | --- | --- |
| `primary` | 주요 액션·강조 | 베이지/브라운 계열 5단계 (`50 ~ 900`) |
| `neutral` | 텍스트·배경·구분선 | 회백색 계열 6단계 (`0 ~ 900`) |
| `semantic.error` | 에러·파괴적 액션 | 레드 계열 |
| `semantic.success` | 성공 피드백 | 그린 계열 |
| `overlay` | 모달 배경 | 반투명 블랙 |

### 6-2. 감정(Mood) 컬러 토큰

| Mood | 한글 | 이모지 | 배경 계열 | 텍스트 계열 |
| --- | --- | --- | --- | --- |
| `joy` | 기쁨 | 😊 | 연노랑 | 골드 |
| `calm` | 평온 | 😌 | 연민트 | 그린 |
| `complex` | 복잡함 | 😐 | 연회색 | 다크그레이 |
| `tired` | 피곤함 | 😴 | 연보라 | 퍼플 |
| `anxious` | 불안함 | 😟 | 연레드 | 레드 |
| `sad` | 슬픔 | 😢 | 연블루 | 네이비 |

### 6-3. 타이포그래피 토큰

| 토큰 | 폰트 | 사용처 |
| --- | --- | --- |
| `font.serif` | Noto Serif KR | 일기 본문 (`BaseTextarea`) |
| `font.sans` | Pretendard | UI 전반 |
| 사이즈 | px | 사용처 |
| --- | --- | --- |
| `text.xs` | 12 | 날짜, 글자 수 카운터 |
| `text.sm` | 14 | 카드 미리보기, 레이블 |
| `text.base` | 16 | 기본 본문 |
| `text.lg` | 18 | 소제목 |
| `text.xl` | 22 | 화면 제목 |
| `text.2xl` | 28 | 홈 CTA |
- 줄 간격: `1.4 (tight)` / `1.7 (normal — 일기 본문)` / `2.0 (loose)`

### 6-4. 간격 · 반경 · 이펙트 토큰

| 구분 | 토큰 | 값 |
| --- | --- | --- |
| 간격 | `spacing.1 ~ spacing.16` | 4px 기반 스케일 (`4px ~ 64px`) |
| 반경 | `radius.sm / md / lg / full` | `6px / 12px / 16px / 9999px` |
| 그림자 | `shadow.sm / md / lg` | 3단계 |
| 트랜지션 | `transition.fast / normal` | `120ms / 200ms` |

## 7. 공통 컴포넌트

### 계층 규칙

| 계층 | Store 접근 | 역할 |
| --- | --- | --- |
| --- | :---: | --- |
| Base | ✗ | 토큰 소비, 순수 UI 단위 |
| Domain (Diary) | ✗ | Base 조합, Props/Emit으로만 통신 |
| Layout | 제한적 | `auth.store.isLoggedIn` 한정 |
| Page | ✓ | Store + Composable 자유 사용, 컴포넌트 조합 |

### BaseButton

| 항목 | 내용 |
| --- | --- |
| 역할 | 서비스 내 모든 버튼의 기본 단위 |
| Props | `variant / size / disabled / loading / fullWidth` |
| Variants | `primary` / `ghost` / `danger` |
| Sizes | `sm / md / lg` |
| loading 상태 | 텍스트 대신 스피너 표시 + 클릭 이벤트 차단 |
| disabled 상태 | `opacity 0.4`  • `cursor not-allowed` |

### BaseTextarea

| 항목 | 내용 |
| --- | --- |
| 역할 | 일기 본문 입력 및 읽기 전용 표시 |
| Props | `modelValue / placeholder / maxLength(5000) / showCounter / readonly` |
| 폰트 | `font.serif` 적용 |
| 높이 | 최소 200px, 내용 따라 auto-grow |
| 카운터 | 우하단 고정, 현재 글자 수 / 5000 |
| readonly | 상세 읽기 화면에서 사용, 스타일 동일·편집만 불가 |

### BaseInput

| 항목 | 내용 |
| --- | --- |
| 역할 | 단일 텍스트 입력 |
| Props | `modelValue / placeholder / error(문자열)` |
| 에러 상태 | 하단 에러 텍스트 + 레드 보더 |

### BaseDialog

| 항목 | 내용 |
| --- | --- |
| 역할 | 확인/취소가 필요한 모달 |
| Props | `open / title / confirmText / cancelText / variant / loading` |
| Emits | `confirm / cancel` |
| 슬롯 | 본문 텍스트 또는 추가 UI 삽입 |
| 오버레이 | 클릭 시 닫힘 (`cancel emit`) |
| 버튼 배치 | 우정렬, 취소 → 확인 순서 |
| 모바일 | 하단 슬라이드업 |

### BaseToast

| 항목 | 내용 |
| --- | --- |
| 역할 | 성공/에러 피드백 알림 |
| Props | `message / type / duration(기본 3000ms)` |
| 위치 | 화면 하단 중앙 고정 |
| 애니메이션 | fade + slide-up |
| 인스턴스 | 앱 전역 1개만 배치 |

### BaseBadge

| 항목 | 내용 |
| --- | --- |
| 역할 | 감정(Mood) 뱃지 시각화 |
| Props | `mood(Mood 타입)` |
| 스타일 | mood 컬러 토큰 자동 적용 |
| 표시 | 이모지 + 한글 레이블 |

### BaseEmptyState

| 항목 | 내용 |
| --- | --- |
| 역할 | 데이터 없을 때 빈 상태 UI |
| Props | `message / actionText` |
| Emits | `action` |

### DiaryCard

| 항목 | 내용 |
| --- | --- |
| 역할 | 일기 목록 카드 단위 |
| Props | `entry(DiaryEntry)` |
| 표시 요소 | 날짜 / BaseBadge / 본문 첫 40자 |
| 인터랙션 | 카드 전체 클릭 → `/diary/:id` 이동 |
| Hover | shadow 강화 + 배경색 변경 |
| 내부 사용 | `BaseBadge` |

### DiaryMoodPicker

| 항목 | 내용 |
| --- | --- |
| 역할 | 감정 6개 단일 선택 |
| Props | `modelValue(Mood \ |
| Emits | `update:modelValue` |
| 동작 | 같은 감정 재클릭 → 선택 해제 (`null`) |
| 레이아웃 | 2행 3열 그리드 |
| 선택 상태 | mood 컬러 배경 + primary 테두리 강조 |
| 내부 사용 | `BaseBadge` |

### DiaryMonthNav

| 항목 | 내용 |
| --- | --- |
| 역할 | 월 이동 네비게이션 |
| Props | `year / month / disableNext` |
| Emits | `prev / next` |
| 표시 | `< 2026년 2월 >` |
| 제약 | 미래 이동 차단 |

### DiaryQuestionBanner

| 항목 | 내용 |
| --- | --- |
| 역할 | 작성 화면 상단 오늘의 질문 표시 |
| Props | `question(string)` |
| 스타일 | `primary.50` 배경, `font.serif`, 좌측 컬러 보더 |

### AppHeader

| 항목 | 내용 |
| --- | --- |
| 역할 | 상단 공통 헤더 |
| Props | `title(string) / showBack(boolean)` |
| 슬롯 | `#right` — 화면별 우측 액션 버튼 삽입 |
| 뒤로가기 | 좌측 화살표 버튼 표시 |

### AppBottomNav

| 항목 | 내용 |
| --- | --- |
| 역할 | 하단 탭 네비게이션 |
| 탭 구성 | 홈(`/home`) / 기록(`/diary`) / 설정(`/settings`) |
| 활성 탭 | 현재 라우트 기반 자동 감지 |
| 렌더 조건 | `auth.store.isLoggedIn === true` |

## 기능 01 — 구글 OAuth 인증

### 개요

| 항목 | 내용 |
| --- | --- |
| 화면 | `pages/index.vue`, `pages/auth/callback.vue` |
| Store | `auth.store — signInWithGoogle() / initSession()` |
| Supabase | `supabase.auth.signInWithOAuth({ provider: 'google' })` |
| 성공 후 | `/home` 이동 |